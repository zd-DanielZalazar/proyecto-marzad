<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<StackPane xmlns:fx="http://javafx.com/fxml"
           fx:controller="com.sga.marzad.controller.InscripcionMateriaController"
           prefWidth="900" prefHeight="600"
           style="-fx-background-color: linear-gradient(to bottom, #1E4370, #0E294B);
                  -fx-padding: 30; -fx-alignment: center;">

    <VBox spacing="24" alignment="TOP_CENTER"
          prefWidth="820" prefHeight="540"
          styleClass="inscripcion-panel">

        <!-- 🏷️ Título -->
        <Label text="Inscripción a Materias"
               styleClass="label-title"/>

        <!-- 🎓 Carrera -->
        <HBox alignment="CENTER" spacing="10">
            <Label text="Carrera:"
                   styleClass="label-subtitle"/>
            <Label fx:id="lblCarrera"
                   style="-fx-font-size: 18px; -fx-text-fill: #12e0c7; -fx-font-weight: bold;"/>
        </HBox>

        <!-- 📘 Selección de materia -->
        <VBox alignment="CENTER" spacing="10">
            <Label text="Seleccionar materia disponible"
                   styleClass="label-subtitle"/>
            <HBox alignment="CENTER" spacing="10">
                <ComboBox fx:id="comboMaterias"
                          prefWidth="360"
                          styleClass="combo-box"/>
                <Button fx:id="btnInscribir"
                        text="Inscribirse"
                        styleClass="btn-primary"/>
            </HBox>
        </VBox>

        <!-- ℹ️ Estado de la materia -->
        <Label fx:id="lblEstado"
               style="-fx-text-fill: #f4f4f4; -fx-font-size: 15px; -fx-font-style: italic;"/>

        <!-- 📋 Tabla de inscripciones -->
        <TableView fx:id="tablaInscripciones"
                   prefHeight="330"
                   prefWidth="780"
                   styleClass="table-view">
            <columns>
                <TableColumn fx:id="colMateria" text="Materia" prefWidth="250"/>
                <TableColumn fx:id="colFecha" text="Fecha Inscripción" prefWidth="180"/>
                <TableColumn fx:id="colEstado" text="Estado" prefWidth="110"/>
                <TableColumn fx:id="colAcciones" text="Acciones" prefWidth="140"/>
            </columns>
            <placeholder>
                <Label text="Aún no tiene inscripciones"
                       style="-fx-text-fill: #cccccc; -fx-font-size: 16px;"/>
            </placeholder>
        </TableView>

    </VBox>
</StackPane>
